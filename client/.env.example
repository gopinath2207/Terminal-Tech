# ── Client Environment Variables ──────────────────────────────────────────────
# Copy this to .env.local and fill in your values

# Your deployed backend API URL (with /api suffix)
# Example: https://your-render-api.onrender.com/api
# Leave as /api in development (Vite proxy handles it automatically)
VITE_API_URL=/api
